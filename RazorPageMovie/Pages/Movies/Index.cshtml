@page
@model RazorPageMovie.Pages.Movies.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>

<form>
    <p>
        Gender: <select asp-for="MoviesGenders" asp-items="Model.Gender">
            <option value="">All</option>
        </select>
        Title: <input type="text" asp-for="searchString"/>
        <input type="submit" value="Filter"/>
    </p>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].RekeaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].Gender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].Raiting)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Movie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RekeaseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.price)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Raiting)
                </td>
            <td>
@*                 <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a> *@

                <a href="/Movies/Edit/?id=@item.Id"> Edit </a> |
                <a href="/Movies/Details/?id=@item.Id"> Details </a>|
                <a href="/Movies/Delete/?id=@item.Id"> Delete </a>
            </td>
        </tr>
}
    </tbody>
</table>
